const hostname = process.env.HOST;
const port = process.env.PORT; 

export const port = ${PORT};
export const host = 'https://${HOST}';
export const awsImage = 'https://s3.eu-central-1.amazonaws.com/pixelistic-js';
